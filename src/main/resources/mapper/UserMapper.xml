<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.belongme.mapper.UserMapper">
<!--    <resultMap id="userMap" type="top.belongme.model.pojo.user.User" autoMapping="true"></resultMap>-->
    <select id="getUserByCreatorId" resultType="java.lang.String">
        select name from job_user where id = #{creatorId}
    </select>
</mapper>
